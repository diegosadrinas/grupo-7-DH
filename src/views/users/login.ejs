<!DOCTYPE html>
<html lang="en">
<head>
    <%-include('../partials/head')%>
    <link rel="stylesheet" href="css/login.css">
    <title>Woodstack | Iniciar Sesion</title>
</head>
<body>
  <%-include('../partials/header')%>
    <main>
      <div class="first-banner"></div>
      <div class="tarjeta">
        <h1>Iniciar Sesion</h1>

        <form method="POST" action="/login" class="loginForm">
         
          <!-- <div class="errores">
            <ul></ul>
          </div> -->

          <div class="campo">
            <!-- puedo cambiar el type a text para q no valide desde el Form -->
            <input type="email" name="email" class="email-input">
            <% if (locals.errors && errors.email) { %>
              <div class="text-danger"> 
              <%= errors.email.msg %>
            </div>
            <% } %>
            <span class="span-email text-danger"></span>
            <label>Email</label>
          </div>

          <div class="campo">
            <input type="password" name="password" class="password">
            <% if (locals.errors && errors.password) { %>
              <div class="text-danger">
                <%= errors.password.msg %> 
              </div>
            <% } %>
            <span class="span-password text-danger" ></span>
            <label>Password</label>
          </div>
          <div class="pass">Olvidó su contraseña?</div>
          <input type="submit" value="Iniciar Sesion">
          <input type="checkbox" name="remember_user">Recordar
          <div class="registrarse">
            No es miembro?<a href="/register">Registrarse</a>
          </div>
        </form>
      </div>
    </main>
    <%-include('../partials/footer')%>

    <script src="/js/loginValidationFront.js"></script>

</body>
</html>